//var gulpMacros = require('./gulp-macros.sjs'),
var assert = require('assert');

describe('gulp-macros', function() {
  beforeEach(function() {
  });

  it('should be ready for some hacking', function() {
    assert.equal(true, true);
  });
});
